// Re-export types from all packages
import { MicManager } from './packages/mic_manager';

// Export all types from MicManager
export * from './packages/mic_manager';

// Export default object with all components
export default {
    MicManager
};